{
  "name": "json-server-demo",
  "version": "1.0.0",
  "description": "demo how to use json-server",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data && node main.js json-server --watch db.json",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "hoang",
  "license": "MIT",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.17.3",
    "query-string": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
